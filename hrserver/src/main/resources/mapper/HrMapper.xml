<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="com.cn.ncvt.mapper.HrMapper">
	    <select id="selectByUserName"  parameterType="string" resultType="Hr">
			select * from hr where username=#{0}
	    </select>
	</mapper>

